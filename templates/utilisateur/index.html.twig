{% extends 'base.html.twig' %}

{% block sidebar %}
<aside id="sidebar" class="sidebar">

                <ul class="sidebar-nav" id="sidebar-nav">
                    <li class="nav-item">
                        <a class="nav-link collapsed" data-bs-target="#tables-nav" data-bs-toggle="collapse" href="#">
                            <i class="bi bi-layout-text-window-reverse"></i><span>gestion utilisateurs</span><i class="bi bi-chevron-down ms-auto"></i>
                        </a>
                        <ul id="tables-nav" class="nav-content collapse " data-bs-parent="#sidebar-nav">
                            <li>
                                <a href="{{ path('ajouter') }}">
                                    <i class="bi bi-circle"></i><span>Ajouter</span>
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('table') }}">
                                    <i class="bi bi-circle"></i><span>Consulter</span>
                                </a>
                            </li>
                        </ul>
                    </li>
                    
                </ul>

            </aside>
{% endblock %}
{% block body %}
    <div class="card-body" style="display:flex;align-items: center;justify-content: center;margin-top: 150px;padding: 10px;margin-left: 210px">

        <h5 class="card-title">Table User</h5>


        <!-- Dark Table -->
        <table class="table table-dark">
            <thead>
            <tr>
                <th scope="col">Email</th>
                <th scope="col">Nom</th>
                <th scope="col">Prenom</th>
                <th scope="col">Numéro</th>
                <th scope="col">Role</th>

                <th scope="col">Modification</th>
                <th scope="col">Suppression</th>
            </tr>
            </thead>
            <tbody>
            {% for user in f %}
                <tr>
                    <td>{{ user.Email }}</td>
                    <td>{{ user.Nom }}</td>
                    <td>{{ user.Prenom }}</td>
                    <td>{{ user.Numtel }}</td>
                    <td>{{ user.Roles }}</td>

                    <td>
                        <a class="btn btn-success" href="{{ path('modifier',{'id':user.Id}) }}">Modifier</a>
                    </td>
                    <td>
                        <a class="btn btn-danger" href="{{ path('suppuser',{'id':user.Id}) }}">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}

            </tbody>
        </table>
        <!-- End Dark Table -->

    </div>
{% endblock %}
