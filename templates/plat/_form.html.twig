{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
    
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 150px; padding: 10px;">

            <div class="card">
                <div class="card-body">
      <a href="{{ path('plat_index') }}"  class="ri-arrow-left-circle-fill" style="font-size: 50px"></a>



                    <!-- General Form Elements -->
                    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
    {{ form_widget(form) }}
 <label for="inputText" class="col-sm-4 col-form-label"></label>
                        <div class="col-sm-10">
                            {{ form_widget(form.description,{'attr':{'class':'form-control'}}) }}

                        </div>

                    {{ form_errors(form.description,{'attr':{'class':'text-danger'}}) }}

  <label for="inputText" class="col-sm-4 col-form-label"></label>
                        <div class="col-sm-10">
                            {{ form_widget(form.nom,{'attr':{'class':'form-control'}}) }}

                    </div>
                    {{ form_errors(form.nom,{'attr':{'class':'text-danger'}}) }}


    <button class="btn btn-info">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


                </div>
            </div>

        </div>

    </div>


{% endblock %}