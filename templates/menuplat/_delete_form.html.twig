<form method="post" action="{{ path('menuplat_delete', {'id': menuplat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menuplat.id) }}">
    <button class="btn">Delete</button>
</form>
